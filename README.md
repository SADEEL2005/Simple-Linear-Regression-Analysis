# Simple-Linear-Regression-Analysis
This repository contains a foundational Machine Learning project that implements a **Simple Linear Regression** model using Python. The primary objective is to analyze the linear relationship between a single independent feature (`x`) and a continuous dependent target variable (`y`), and to build a predictive model based on this relationship.
